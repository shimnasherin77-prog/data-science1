# 8. Given:
# A = np.array([[3, 1], [0, 2]])
# B = np.array([[4], [5]])
# → Perform matrix multiplication (A @ B) and explain the result.
import numpy as np
A = np.array([[3, 1], [0, 2]])
B = np.array([[4], [5]])
print(A@B)
#In this,1st row in A and 2 col in B are multiple and then sum the values and same like 
# these,2 nd row in A and 2 col in B are multiple and then sum the value